# Herbier Gourmand v2.9.7.15.4 — Recherche flottante Recettes

Base : v2.9.7.15.3 validée.

Modification unique : dans **Recettes**, après avoir fait défiler la page au point que la zone de recherche n'est plus visible, un bouton flottant **🔍 Rechercher** apparaît en bas à droite. Il ramène à la recherche existante et place le curseur dans le champ.

Aucune interception de la navigation ou de `switchView()` ; aucun fichier de données modifié.

Fichiers à remplacer : `index.html`, `app-v282.js`, `version.json`, `sw.js`, `service-worker.js`.
